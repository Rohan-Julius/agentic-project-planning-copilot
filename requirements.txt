# Mirrors pyproject.toml [project.dependencies]. Install with:
#   pip install -r requirements.txt
# For editable install + dev tools (pytest, ruff), prefer:
#   pip install -e ".[dev]"

# --- Backend / API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9

# --- Agent orchestration + local LLM ---
langgraph>=0.2
ollama>=0.3

# --- RAG (embeddings, vector store, hybrid retrieval) ---
sentence-transformers>=3.0
qdrant-client>=1.9
rank-bm25>=0.2.2

# --- Document parsing ---
pymupdf>=1.24
python-docx>=1.1

# --- Storage ---
sqlalchemy>=2.0
